// The booking sheet, reachable from a file (reschedule) as well as from the
// shops stack. Same component, second route: pushing a route that lives in
// another tab's stack would switch tabs.
export { default } from '../../shops/garage/booking';
