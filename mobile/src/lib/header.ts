// expo-router 57 vendors react-navigation; `useHeaderHeight` is not re-exported
// publicly yet, so the deep import lives here — one place to change when it is.
export { useHeaderHeight } from 'expo-router/build/react-navigation/elements/Header/useHeaderHeight';
